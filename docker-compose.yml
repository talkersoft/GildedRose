version: "3"
services:
    web:
        build: .
        ports:
            - "8080:5000"
        depends_on:
            - db
    db:
        ports:
            - "1433:1433"
        image: "mcr.microsoft.com/mssql/server:2017-latest"
        environment:
            SA_PASSWORD: "Passw0rd!"
            ACCEPT_EULA: "Y"
            SQLVOLUME: "/var/volumes/"